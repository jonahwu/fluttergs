import 'package:flutter/material.dart';
import 'dart:io';

class FFetchData extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    sleep(const Duration(seconds: 3));

    return null;
  }
}
